services:
  invoice-app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app 
    command: >
      sh -c "python run_pipeline.py &&
             streamlit run dashboard.py --server.port=8501 --server.address=0.0.0.0"